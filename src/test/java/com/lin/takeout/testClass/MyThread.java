package com.lin.takeout.testClass;

public class MyThread extends Thread{
    private long id;
/*    @Override
    public void run() {

        GetIdByThreadLocal.getThreadLocal(id);
        System.out.println(GetIdByThreadLocal.getId());
    }

    public void setId(long id) {
        this.id = id;
    }*/
}
